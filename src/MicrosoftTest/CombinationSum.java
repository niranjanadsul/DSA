package MicrosoftTest;

public class CombinationSum {
    //An array generator service takes in a single
    // integer k and a sum s. it returns an array with a sum s where i th element is k+i-1.
    //given an integer s , find the number of valid values of k for which it is
    // possible to generate a valid array}
}